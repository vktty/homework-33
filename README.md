function compiletailwind connects to the library, adds styles and prefixes, then adds a new css file to the main css folder
function compilecss converts files from 'scss' to 'css'
function cssmin takes all files with extention css and minifies them
function jsmin takes all files with extention js and minifies them
function clean deletes files
function watchTask watches main files and applies changes using previous functions 
function live watches, displays and shows changes immediately in browser of all files
